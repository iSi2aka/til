fruites = [apple, orange, lemon]

fruites.forEach(fruite -> System.out.println(fruite));    //1行の変更ならばアローを使用して簡略化可能

furites.forEach(fruite -> {
    String furuiteUpper = fruite.toUpperCase());
    System.out.println(fruiteUpper);
});                                                       //2行以上の変更ならば{}を使ってインデントを使って記述する
